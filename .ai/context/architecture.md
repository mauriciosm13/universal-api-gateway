# Architecture Context

Hexagonal Architecture (Ports & Adapters). Cloud-agnostic. Core modules: auth, routing, ratelimit, middleware, observability.

Current v0.1: `cmd/gateway`, `internal/config`, `internal/server` with health endpoints.

See [docs/ARCHITECTURE.md](../../docs/ARCHITECTURE.md).
