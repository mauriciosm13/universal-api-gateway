# Security

- JWT validation
- OAuth2 / OpenID Connect
- mTLS
- OWASP best practices
- Secrets via environment or secret managers (never in repo)
- Input validation on all external input
- Rate limiting as a security control
